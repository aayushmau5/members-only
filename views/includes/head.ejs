<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <header>
      <div class="logo">MembersOnly</div>
      <div class="links">
        <a href="/" class="<%= path === '/' ? 'active' : '' %>">Home</a>
        <% if(authenticated) { %>
          <a href="/logout">Logout</a>
          <% if(!isMember) { %>
            <a href="/join" class="<%= path === '/join' ? 'active' : '' %>">Join</a>
          <% } else { %>
            <a href="/add-message" class="<%= path === '/add-message' ? 'active' : '' %>">Add Message</a>
          <% } %>
        <% } else { %>
          <a href="/login" class="<%= path === '/login' ? 'active' : '' %>"
            >Login</a
          >
          <a href="/signup" class="<%= path === '/signup' ? 'active' : '' %>"
            >Signup</a
          >
        <% } %>
      </div>
    </header>
  </body>
</html>
